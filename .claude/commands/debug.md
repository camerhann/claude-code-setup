Help me debug this issue. Follow a systematic approach:

1. **Understand the symptom** — read any error messages, stack traces, or descriptions I provide
2. **Locate the code** — find the relevant source files, trace the execution path
3. **Form hypotheses** — list 2-3 most likely root causes based on the code
4. **Verify** — read the surrounding code, check for common pitfalls (off-by-one, null refs, async issues, wrong variable scope, stale state)
5. **Fix** — propose a targeted fix, explain why it works
6. **Prevent** — suggest if a test could catch this regression in future

If I provide a stack trace, start from the deepest application frame (skip framework internals).
If I describe a symptom without an error, search the codebase for the relevant code path first.

Don't guess — read the code before diagnosing.

$ARGUMENTS
